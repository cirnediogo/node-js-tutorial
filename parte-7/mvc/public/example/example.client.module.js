/*
This will create an AngularJS module, but it is still needed to include the module file
in the application page and the module as a dependency of the main application module.
*/
angular.module('example', []);